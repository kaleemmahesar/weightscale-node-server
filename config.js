module.exports = {
    wsPort: 9091,
    httpPort: 9090,
    serialPortPath: "COM4",
    baudRate: 9600,        // match your indicator
    dataBits: 8,           // add if needed
    parity: "none",        // add if needed
    stopBits: 1,           // add if needed
};
